# Function: salt()

> **salt**(`length`): `string`

Defined in: [hash.ts:7](https://github.com/epikodelabs/actionstack/blob/v3/projects/libraries/actionstack/src/lib/hash.ts#L7)

Generates a random string of a specified length in base-36 (including digits and lowercase letters).

## Parameters

### length

`number`

The desired length of the random string.

## Returns

`string`

- A random base-36 string of the provided length.
