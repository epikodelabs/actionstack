# Type Alias: Reducer()\<T\>

> **Reducer**\<`T`\> = (`state`, `action`) => `T`

Defined in: [types.ts:164](https://github.com/epikodelabs/actionstack/blob/v3/projects/libraries/actionstack/src/lib/types.ts#L164)

A function that takes the current state and an action, and returns
the updated state (excluding promises).

## Type Parameters

### T

`T` = `any`

## Parameters

### state

`T`

### action

[`Action`](../interfaces/Action.md)

## Returns

`T`
